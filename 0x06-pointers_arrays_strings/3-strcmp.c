#include "main.h"

/**
 * _strcmp - function that compares two strings
 * @s1: string one
 * @s2: string two
 * Return: comparison
 */
int _strcmp(char *s1, char *s2)
{
	int comp;

	comp = strcmp(s1, s2);
	return (comp);
}
