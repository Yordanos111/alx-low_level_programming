#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: string
 * Return: lrngth of the string
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
