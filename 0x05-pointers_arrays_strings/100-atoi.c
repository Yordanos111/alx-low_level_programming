#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _atoi - convert a string to an integer
 * @s: string to be converted to intigers
 * Return: iniger
 */
int _atoi(char *s)
{
	int i;

	i = atoi(s);
	return(i);
}
